#include <stdio.h>

/**
 * main - Entry point
 * puts writes argument in char 'str' to the output stream.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
